# Проект: Кейлоггер с AI-анализом настроения

Комплексная система для сбора нажатий клавиш, анализа эмоционального состояния пользователя с помощью локальной языковой модели (LLM) и визуализации результатов на веб-дашборде.

---

## 🚀 Основные возможности

-   **Сбор нажатий клавиш:** Фоновая C-программа перехватывает ввод с клавиатуры.
-   **Шифрование "на лету":** Локальный лог-файл (`keylog.xor`) шифруется с помощью XOR-шифра для базовой безопасности.
-   **Централизованное хранение:** Все собранные слова сохраняются в базе данных MySQL.
-   **AI-анализ настроения:** Python-сервис периодически запрашивает данные, отправляет их в локальную LLM (через LM Studio) для анализа и записывает результат.
-   **Адаптивный веб-дашборд:** Django-дашборд визуализирует проанализированные данные в трех колонках:
    -   Лента последних эмоциональных состояний.
    -   Облако самых популярных слов.
    -   Адаптивная гистограмма частоты использования слов.

---

## 🛠️ Структура проекта

Проект разделен на три основных компонента:

1.  **`keylogger/` (C-программа)**
    -   Ядро системы, непосредственно кейлоггер.
    -   Отвечает за перехват, шифрование и отправку данных в БД.
    -   Настраивается через файл `config.ini`.

2.  **`analyze_engine/` (Python-сервис)**
    -   "Мозг" проекта.
    -   Содержит скрипт `main.py`, который по расписанию анализирует данные.
    -   Настраивается через файл `config.yaml`.

3.  **`analyze_engine/dashboard/` (Django-приложение)**
    -   Веб-интерфейс для визуализации результатов.
    -   Представляет данные в чистом, адаптивном трехколоночном макете.

---

## ⚙️ Установка и запуск

### Предварительные требования

-   C-компилятор и **CMake** (например, MinGW в Windows).
-   **MySQL Server** с созданной базой данных и пользователем.
-   **Python 3.x** и Pip.
-   **LM Studio** (или другой локальный OpenAI-совместимый сервер).

### 1. C-Кейлоггер

1.  **Настройте `config.ini`:** Укажите учетные данные для подключения к MySQL и ключ шифрования.
2.  **Соберите проект:** Откройте проект в IDE с поддержкой CMake (например, CLion) и выполните сборку. CMake автоматически найдет зависимости и создаст исполняемый файл.
3.  **Запустите `keylogger.exe`** из папки сборки (`cmake-build-debug`).

### 2. Python-сервис анализа

1.  **Настройте `config.yaml`:** Укажите учетные данные для подключения к MySQL и отредактируйте промпт для LLM при необходимости.
2.  **Установите зависимости:**
    ```sh
    pip install mysql-connector-python openai schedule pyyaml
    ```
3.  **Запустите сервис:**
    ```sh
    cd analyze_engine
    python main.py
    ```

### 3. Django-дашборд

1.  **Установите Django:**
    ```sh
    pip install Django
    ```
2.  **Запустите веб-сервер:**
    ```sh
    cd analyze_engine/dashboard
    python manage.py runserver
    ```
3.  Откройте в браузере `http://127.0.0.1:8000/` для просмотра дашборда.